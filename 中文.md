# DigitalFilm

> 
> 基于物理分解与神经网络的端到端胶片美学模拟系统

DigitalFilm 是一套完整的胶片风格模拟解决方案，融合可微分物理渲染管线与神经网络训练，为数字图像赋予真实胶片质感；同时提供专业级 Web 编辑工作台与开发者友好的 MCP 集成能力，兼顾普通用户的调色需求与开发者的自定义扩展空间。

![Output](./example/digital_output.svg)

## ✨ 核心特性

- 🎞️ **物理级胶片模拟**：v2 管线将胶片成像拆解为曝光、染料耦合、光晕扩散、影调曲线、颗粒等可微分阶段，参数可解释、易人工调试
- ⚡ **轻量高效推理**：以矩阵、1D/3D LUT 替代大型生成网络，显存占用低，可在消费级 GPU 实现实时渲染
- 🖥️ **全功能 Web 工作台**：配套完整图像编辑应用，覆盖基础调色、胶片预设、景深模拟、画质增强全流程
- 🔌 **MCP 协议原生支持**：内置模型上下文协议服务，可无缝接入 AI 代理与智能工作流
- 🧠 **可端到端训练**：支持自定义数据集训练专属胶片风格，近恒等初始化保障小样本下训练稳定

---

## 📋 环境依赖

运行本项目需要安装以下基础环境：

- Python 3.10+
- Go 1.20+
- Node.js 16+（前端开发 / 构建）
- pnpm / npm / yarn

安装 Python 依赖：

```
pip install -r requirements.txt
```

---

## 🚀 快速开始

### 1. 快速体验 Demo

仅运行核心胶片模拟能力，无需启动完整前后端：

```
python demo.py
```

### 2. 测试 MCP 服务

验证 MCP 协议连通性：

```
npx @modelcontextprotocol/inspector --transport stdio -- python app/backend/mcp_server.py
```

### 3. 启动完整 Web 应用

推荐按以下顺序启动全栈服务：

#### 步骤 1：启动图像处理服务

负责模型推理、图像编辑与风格生成：

```
python app/backend/image_server.py
```

#### 步骤 2：启动主后端

负责项目管理、图片信息、编辑参数、用户权限与前端数据交互：

```
cd app/backend/master_backend
go run .
# 或编译后运行
go build -o master_backend
./master_backend
```

#### 步骤 3：启动静态资源后端

负责托管原始上传图片与生成结果，提供 HTTP 访问链路：

```
cd app/backend/static_backend
go run .
# 或编译后运行
go build -o static_backend
./static_backend
```

#### 步骤 4：启动前端应用

```
cd app/frontend
npm run dev
# 或使用 pnpm
pnpm dev
```

启动完成后即可在浏览器中访问 DigitalFilm 编辑工作台。

> 
> 💡 初始化管理员账号：首次部署后，可执行以下 SQL 设置首位管理员用户
> 
> 
> ```
> UPDATE users SET is_admin = 1 WHERE username = '你的用户名';
> ```

---

## 🎞️ digitalFilm v2 物理渲染管线

digitalFilm v2 是项目的核心技术模块，采用**物理分解式可微分渲染管线**设计，解决了 v1 全卷积 GAN 方案显存开销大、可解释性弱、小样本训练困难的问题。

![digitalFilmv2](./example/digitalFILm.png)

### 设计思路

参考计算光学胶片模拟理念（致敬 Phos 项目），将胶片成像的物理过程拆解为独立的可微分阶段，每个阶段对应真实胶片的一个成像环节；所有模块初始化为接近恒等映射，从残差开始学习风格偏移，既保证物理可解释性，也大幅提升了小数据集下的训练稳定性。

### 完整管线流程

管线分为**线性域**与**曲线域**两个处理阶段：

#### 线性域处理（光学物理阶段）

1. **曝光校正 ExposureModule**：逐通道曝光增益与偏置调节，模拟胶片曝光补偿
2. **光谱染料混色**：提供两种可选模式
   - `linear` 轻量模式：3×3 光谱混色矩阵 + 残差 3D LUT，兼顾速度与基础色彩耦合效果
   - `density` 物理模式（Phos 风格）：密度域线性染料串扰 + 小尺寸密度 3D LUT，更贴合胶片减色混合的物理规律
3. **金字塔光晕 PyramidBloom**：多尺度高斯金字塔模拟光线在乳剂层的散射，支持波长依赖的散射半径（红光散射最远、蓝光最近），还原真实胶片的高光溢出现象

#### 曲线域处理（冲印化学阶段）

4. **色调响应曲线 ToneResponseCurve**：逐通道 1D LUT 模拟胶片 H&D 特性曲线，呈现胶片典型的暗部趾部、线性中段与高光肩部影调
5. **残差色彩校正**：小尺寸 3D LUT 补充线性矩阵无法覆盖的非线性色彩交叉效应
6. **胶片颗粒 Grain**：多尺度亮度调制噪声，暗部颗粒更显著，还原真实胶片的颗粒质感与有机纹理

### 技术优势

- 全流程可微分，支持端到端训练
- 参数具备明确物理含义，便于人工微调与风格控制
- 计算开销远低于全卷积生成模型，支持实时推理
- 近恒等初始化，小样本数据集下训练不易崩坏

---

## 🖥️ Web 编辑应用

### 核心功能

- **基础调色工具**：曝光、对比度、高光 / 阴影、色温 / 色调、饱和度全参数调节
- **胶片风格处理**：内置 digitalFilm v2 多套胶片预设，可独立控制颗粒强度、高光溢出强度，支持高级色彩分级
- **景深模拟**：基于 DeepAnything2 的深度估计，模拟大光圈浅景深虚化效果
- **画质增强**：基于 OSEDiff 的画质修复、细节增强与降噪

---

## 🔌 MCP 服务

项目内置 MCP（Model Context Protocol）服务，可将 DigitalFilm 的胶片模拟与图像编辑能力无缝接入支持 MCP 协议的 AI 应用、智能代理系统，构建自动化智能图像工作流。

启动命令：

```
python app/mcp_server.py
```

---

## 🧠 模型训练

训练入口为 `pipeline.py`，可通过修改 `options/` 目录下的配置文件自定义训练参数：

- 数据集路径与数据加载策略
- Batch Size、学习率、迭代次数等训练超参
- LUT 维度、basis 数量等模型结构参数
- 是否启用 3D/4D LUT、残差连接、混合模式等特性

启动训练：

```
python pipeline.py
```

---

## 🗺️ 发展路线图

✅ **已完成**

- 基于物理分解的 digitalFilm v2 渲染管线
- 景深模拟功能
- 画质增强能力
- MCP 协议服务支持

📌 **规划中**

- 历史版本系统：全编辑步骤的撤销 / 重做
- 预设管理：自定义预设的保存、导入与分享
- 批量处理：多图像批量应用风格与调色参数
- 高级调色工具：曲线、色阶、HSL 独立调节
- Agent 智能编辑：基于 MCP 的 AI 代理驱动自动调色
- 插件系统：支持第三方功能扩展
- RAW 文件原生支持：直接处理 RAW 格式底片

---

## 🤝 贡献指南

欢迎参与项目贡献，步骤如下：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

---

## 📄 许可证

本项目基于 GPL-v3 许可证开源，详见 [LICENSE](LICENSE) 文件。

---

## 🙏 致谢

- Vue.js/ Vite / TypeScript 前端技术栈
- 所有为本项目贡献代码与建议的开发者
- [Phos](https://link.wtturl.cn/?target=https%3A%2F%2Fgithub.com%2FZacharyHu0%2FPhos&scene=im&aid=497858&lang=zh) 项目提供的计算光学胶片模拟思路

---

## 👤 作者

- **SongZihui-sudo** - *初始工作* - \[SongZihui-sudo\]

⭐ 如果这个项目对你有帮助，欢迎点一个 Star 支持我们！
