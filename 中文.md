# DigitalFilm 数字暗房系统

> 基于神经网络模拟传统胶片美学的数字暗房系统

![应用界面](example/app.png)

DigitalFilm 是一个专注于胶片风格模拟的项目，包含以下核心组件：

- **模型训练管线** - 基于神经网络的胶片风格迁移
- **MCP服务** - 面向AI应用的模型上下文协议集成
- **现代化Vue 3 Web应用** - 专业照片编辑界面
- **多服务后端架构** - 主后端、静态资源后端、图像处理服务

项目目标是通过神经网络为数字图像赋予胶片质感，同时提供专业且实用的编辑工作流程。

---

## 快速开始

运行 demo   
```bash
python demo.py
```

测试 mcp  
```bash
npx @modelcontextprotocol/inspector --transport stdio -- python mcp_server.py
```

## 功能概览

- 使用神经网络学习数字图像到胶片风格图像的映射
- 支持基础图像编辑参数预览与保存
- 支持项目 / 图片管理
- 支持图片上传、静态托管、结果图访问
- 支持通过 MCP 接入到其他 AI 应用中使用
- 支持通过独立图像服务执行图像生成与处理

---

## 项目结构

一个典型的结构如下：

```text
DigitalFilm/
├── app/                    # DigitalFilm 应用前端 / 桌面端相关代码
├──────── master_backend/         # 主后端，负责项目、图片、参数等数据管理
├──────── static_backend/         # 静态资源后端，负责托管上传图像和生成结果
├──────── image_server.py         # Python 图像处理服务
├── pipeline.py             # 训练入口
├── mcp_server.py           # MCP 服务入口
├── options/                # 模型与训练配置
├── example/                # 示例图片
└── ...
```

## 环境依赖
使用本项目需要安装：

Python
Go
前端部分如果在 app/ 中使用了vue，还需要：

Node.js
pnpm / npm / yarn

## 模型训练
训练入口为：

```BASH
python pipeline.py
```
你可以通过修改配置文件来调整训练参数，例如：

数据集路径  
batch size  
学习率  
LUT 维度  
是否启用 3D / 4D LUT  
basis 数量  
residual / blend 等选项

## MCP 服务
项目提供了 MCP 服务，可以供其他 AI 应用接入使用。

启动方式：

```BASH
python mcp_server.py
```
启动后，你可以将该 MCP 服务接入支持 MCP 的 AI 应用或代理系统中，用于调用 DigitalFilm 的能力。

## 应用启动方式
app/ 内提供了 DigitalFilm 应用，可以支持：

简单图像编辑  
图片上传与项目管理  
胶片风格转换  
图片参数保存与恢复  
使用前准备  
使用 DigitalFilm App 需要先启动以下服务： 

Python 图像编辑服务器  
Go 主后端  
Go   
1. 启动图像处理服务
图像处理服务负责执行图像生成 / 图像编辑 / 模型推理相关能力。

```BASH
python image_server.py
```

2. 启动主后端
主后端负责：

项目数据管理  
图片信息管理  
编辑参数存储  
预设配置管理  
与前端的数据交互  
进入主后端目录后编译并运行：  


``` BASH
go run .
```
或者先编译：

```BASH
go build -o master_backend
./master_backend
```

3. 启动静态资源后端
静态资源后端负责：  

托管上传的原始图片  
托管生成后的结果图片  
提供 HTTP 访问地址供前端和其他后端使用  
进入静态后端目录后编译并运行：  

```BASH
go run .
```

或者：

```BASH
go build -o static_backend
./static_backend
```
4. 启动 App
如果 app/ 是前端项目：

```BASH
cd app
npm run dev
```
或：

```BASH
pnpm dev
```
启动后即可在浏览器中使用 DigitalFilm 应用。

推荐启动顺序  
建议按以下顺序启动：

```
python image_server.py  
master_backend  
static_backend  
app
```

需要手动执行 `SQL` 来设置第一个管理员用户。
```sql
UPDATE users SET is_admin = 1 WHERE username = '你的用户名'; 
```

## DigitalFilm Web应用详解

### 技术栈
- **Vue 3.4+** - 支持Composition API的渐进式JavaScript框架
- **TypeScript 5.4+** - 类型安全的开发体验
- **Vite 5.2+** - 极速构建工具
- **Pinia 2.3+** - 状态管理
- **Vue Router 4.3+** - 官方路由管理
- **Axios 1.7+** - HTTP客户端

### 前端架构 (`app/frontend/`)

```
src/
├── api/              # API接口层
│   ├── adminApi.ts   # 管理员API
│   ├── client.ts     # HTTP客户端配置
│   ├── imageApi.ts   # 图像操作API
│   ├── projectApi.ts # 项目管理API
│   └── userApi.ts    # 用户认证API
├── components/       # Vue组件
│   ├── common/       # 通用组件（登录模态框、主题切换等）
│   ├── editor/       # 编辑面板（基础调整、胶片风格、导出等）
│   ├── layout/       # 布局组件（侧边栏、预览、面板）
│   └── project/      # 项目组件（缩略图列表、项目列表）
├── composables/      # 组合式函数
│   ├── useAvatar.ts          # 头像生成
│   ├── useFilmGeneration.ts  # 胶片生成
│   ├── useImagePreview.ts    # 图像预览
│   ├── useProjectManager.ts  # 项目管理
│   └── useTheme.ts           # 主题管理
├── stores/          # Pinia状态管理
│   ├── adminStore.ts         # 管理员状态
│   ├── editorStore.ts        # 编辑器状态
│   ├── projectStore.ts       # 项目状态
│   ├── themeStore.ts         # 主题状态
│   └── userStore.ts          # 用户状态
├── views/           # 页面视图
│   ├── DarkroomWorkspace.vue  # 主工作区
│   ├── AdminDashboard.vue    # 管理员控制台
│   └── AdminLogin.vue        # 管理员登录
└── services/        # 业务逻辑服务
    ├── FilmGenerationService.ts   # 胶片生成服务
    ├── ImageEditorService.ts      # 图像编辑服务
    ├── ProjectService.ts          # 项目服务
    └── ThemeService.ts            # 主题服务
```

### 核心功能

#### 1. 专业工作区 (`DarkroomWorkspace.vue`)
- **三栏布局**：左侧项目导航、中央图像预览、右侧编辑工具
- **主题系统**：支持深色/浅色/自动主题，平滑过渡效果
- **响应式设计**：适配不同屏幕尺寸
- **实时预览**：所有调整即时显示效果

#### 2. 照片编辑功能

**基础调整** (`BasicAdjustPanel.vue`)
- 曝光调整 (-100 到 +100)
- 对比度控制
- 高光/阴影调节
- 色温/色调调整
- 饱和度控制

**胶片风格处理** (`FilmStylePanel.vue`)
- 多种胶片预设
- 胶片颗粒效果
- 高光溢出效果
- 高级色彩分级

**前后对比** (`BeforeAfterSlider.vue`)
- 交互式滑块即时对比
- 原图与编辑版本的平滑过渡

**专业导出** (`ExportPanel.vue`)
- 多格式支持
- 质量控制
- 尺寸调整
- 批量导出选项

#### 3. 项目管理
- 创建/删除项目，级联数据管理
- 图像上传，自动分配到项目
- 缩略图网格视图，高效加载
- 项目切换，状态保持

#### 4. 用户系统与管理后台
**用户功能**
- JWT令牌认证
- 基于角色的访问控制（用户/管理员）
- 登录模态框，表单验证
- 会话持久化

**管理员控制台** (`AdminDashboard.vue`)
- 用户CRUD操作
- 密码管理
- 管理员角色授予/撤销
- 用户统计显示
- 权限控制

#### 5. 状态管理系统

**编辑器存储** (`editorStore.ts`)
```typescript
interface EditSettings {
  basic: {
    exposure: number      // 曝光
    contrast: number     // 对比度
    highlights: number   // 高光
    shadows: number      // 阴影
    temperature: number  // 色温
    tint: number         // 色调
    saturation: number   // 饱和度
  }
  film: {
    preset: string       // 预设
    grain: number        // 颗粒
    bloom: number        // 光晕
  }
}
```

**项目存储** (`projectStore.ts`)
- 当前项目管理
- 图像列表及元数据
- 上传状态跟踪
- 选择状态

#### 6. 主题系统
动态CSS变量实现无缝主题切换：
```css
[data-theme="dark"] {
  --bg-app: #1a1a1a;
  --text-primary: #ffffff;
}

[data-theme="light"] {
  --bg-app: #ffffff;
  --text-primary: #1a1a1a;
}
```

### 开发工作流

#### 前端开发
```bash
cd app/frontend

# 安装依赖
npm install

# 开发服务器
npm run dev

# 类型检查和构建
npm run build

# 预览生产构建
npm run preview
```

#### 构建配置
- **Vite配置**：针对Vue 3单文件组件和TypeScript优化
- **TypeScript配置**：严格模式，路径别名
- **生产构建**：压缩、Tree-shaking、资源优化

### API集成

#### HTTP客户端设置
```typescript
// api/client.ts
const client = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// JWT请求拦截器
client.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})
```

#### 服务集成
- **图像服务**：上传、处理和检索
- **项目服务**：项目CRUD操作
- **管理员服务**：用户管理和认证
- **文件服务**：静态资源托管和交付

### 高级功能

#### 用户体验增强
- **键盘快捷键**：键盘控制提高工作效率
- **智能缓存**：图像和设置缓存提升性能
- **响应式布局**：适应不同屏幕尺寸和方向
- **无障碍支持**：符合WCAG标准的界面，适当的ARIA标签
- **离线支持**：渐进式Web应用能力，支持离线编辑

#### 开发特性
- **热模块替换**：即时开发更新
- **TypeScript严格模式**：增强类型安全和开发体验
- **ESLint和Prettier**：代码质量和格式化标准
- **组件测试**：全面的测试覆盖
- **Storybook**：组件文档和开发

#### 性能优化
- **代码分割**：懒加载组件和路由
- **Tree Shaking**：移除未使用的代码
- **图像优化**：高效的图像加载和缓存
- **包大小优化**：最小的生产包大小
- **内存管理**：大图像的高效内存使用

### 未来路线图

#### 计划功能
- [ ] **历史系统**：所有编辑的撤销/重做功能
- [ ] **预设管理**：保存和分享自定义编辑预设
- [ ] **批量处理**：同时对多个图像应用编辑
- [ ] **高级工具**：曲线、色阶和HSL调整
- [ ] **Agent驱动编辑**：通过AI代理和MCP集成实现智能图像编辑
- [ ] **云同步**：跨设备项目同步
- [ ] **协作编辑**：实时协作编辑
- [ ] **移动应用**：原生移动应用程序
- [ ] **插件系统**：第三方插件的可扩展架构
- [ ] **RAW支持**：直接RAW文件处理

## DigitalFilm App 功能说明  

### 核心功能
- ✅ **专业项目管理** - 创建、删除、切换多个摄影项目
- ✅ **高级照片编辑** - 全面的调整面板和实时预览
- ✅ **用户认证系统** - 完整的用户系统和基于角色的访问控制
- ✅ **主题切换** - 深色/浅色主题自动切换
- ✅ **实时预览** - 所有编辑操作的即时视觉反馈
- ✅ **批量操作** - 高效的多图处理工作流
- ✅ **管理后台** - 完整的用户管理界面
- ✅ **响应式设计** - 为各种屏幕尺寸优化

### 编辑能力

**基础调整**
- 曝光调整 (-100 到 +100)
- 对比度控制 (-100 到 +100)
- 高光恢复 (-100 到 +100)
- 阴影增强 (-100 到 +100)
- 色温控制 (-100 到 +100)
- 色调调整 (-100 到 +100)
- 饱和度控制 (-100 到 +100)

**胶片风格处理**
- 多种胶片预设（Kodak、Fuji等）
- 可调节颗粒效果
- 高光溢出模拟
- 高级色彩分级
- 自定义参数保存/加载

**用户界面**
- 三栏工作区布局
- 交互式前后对比滑块
- 悬停效果的缩略图网格
- 直观的滑块控制和数字显示
- 流畅的动画和过渡效果

### 技术亮点

**性能优化**
- 组件懒加载
- 图像缓存策略
- 高效的Pinia状态管理
- 优化的响应式系统
- 内存高效的图像处理

**用户体验**
- JWT令牌认证和自动刷新
- 基于角色的访问控制（管理员/用户）
- 表单验证和错误处理
- 破坏性操作的确认对话框
- 跨页面重载的会话持久化

**开发体验**
- TypeScript提供类型安全
- Composition API实现更好的代码组织
- 模块化架构，清晰的关注点分离
- 可复用的组件库
- 全面的API抽象层

## 模型说明  

digitalFilmv2 是一个轻量级的数字到胶片风格生成模型，核心思路 包括：

Basis 3D LUT mixture
可选 Basis 4D LUT mixture
全局特征网络预测 LUT mixing weights
可选 residual blending
LUT regularization
Total Variation
Monotonicity regularization

模型支持：
- use_3d
- use_4d
- num_basis_3d
- num_basis_4d
- lut3d_dim
- lut4d_dim
- num_context_bins
- learn_blend

其整体目标是以较轻量的方式结合可解释的 LUT 表达能力与神经网络预测能力，实现具有胶片感的数字图像渲染。

模型主要由以下模块构成：

1. GlobalFeatureNet
一个轻量 CNN，用于从输入图像中提取全局特征，并预测：

- 3D LUT basis 权重
- 4D LUT basis 权重
- 分支融合权重

2. BasisLUT3D
学习多个可训练的 3D LUT basis，并通过预测权重进行融合：

- 输出融合后的 3D LUT
- 加上 identity LUT 作为初始基准
- 保持输出在 [0, 1]

3. BasisLUT4D
学习多个可训练的 4D LUT basis，并结合上下文维度进行更复杂的颜色映射。

4. TV / Monotonicity Regularization
为了保证 LUT 的平滑性与合理性，训练中加入：

- TV 正则
- 单调性正则

5. Residual Blending
在最终输出中加入一定比例的输入图像，有助于提升稳定性与自然感：

```TEXT
out = 0.7 * lut_output + 0.3 * input
```

## 贡献指南

1. Fork本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m '添加某个惊人的功能'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

## 许可证

本项目采用MIT许可证 - 详见 [LICENSE](LICENSE) 文件

## 作者

- **你的名字** - *初始工作* - [YourUsername]

## 致谢

- Vue.js 团队
- Vite 团队  
- TypeScript 团队
- 所有贡献者

---

⭐ 如果这个项目对你有帮助，请给一个星标！

## 开发说明

项目目前由多个服务组成，建议在开发时分别调试：

- Python 模型 / 图像服务
- Go 主后端
- Go 静态资源后端
- Vue 3 前端应用

### 调试技巧

1. **服务独立性**：每个服务都可以独立启动和测试
2. **API测试**：可以使用Postman或其他工具测试后端API
3. **前端开发**：Vue 3应用支持热模块替换，开发体验良好
4. **日志查看**：每个服务都有详细的日志输出，便于问题排查

### 常见问题

**Q: 前端无法连接到后端？**
A: 检查`app/frontend/.env`文件中的`VITE_API_BASE_URL`配置是否正确。

**Q: 图像处理失败？**
A: 确保Python图像服务正常运行，检查模型文件是否加载成功。

**Q: 管理员权限无法设置？**
A: 需要直接在数据库中执行SQL语句来设置第一个管理员用户。
